name: Deploy remote parent dashboard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment routing
        run: echo "M5 Pages deployment is intentionally run from main so the github-pages environment can authorize it. The main workflow checks out task/008-remote-control."
